# CollectiVAI Router – .env.example
# ---------------------------------
# Template für lokale Entwicklung.
# WICHTIG:
# - Diese Datei enthält NUR Platzhalter.
# - Lege dir lokal eine .env an: cp .env.example .env
# - Trage NUR Test- und Dev-Keys in .env ein, niemals Produktiv-Keys.
#
# Diese .env wird von router/config.py geladen.

# --- OpenAI ---
# Beispiel: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_API_KEY="your-openai-api-key"

# Optional, falls du einen eigenen Proxy / anderes Endpoint nutzt:
# OPENAI_BASE_URL="https://api.openai.com/v1"


# --- Gemini (Google AI Studio / Vertex kompatibel) ---
GEMINI_API_KEY="your-gemini-api-key"
# Optional: eigenes Endpoint / Region
# GEMINI_BASE_URL="https://generativelanguage.googleapis.com/v1beta"


# --- Mistral AI ---
MISTRAL_API_KEY="your-mistral-api-key"
# MISTRAL_BASE_URL="https://api.mistral.ai/v1"


# --- Meta (LLaMA / kompatibler Provider) ---
META_API_KEY="your-meta-api-key"
# META_BASE_URL="https://api.meta.ai/v1"


# --- DeepSeek ---
DEEPSEEK_API_KEY="your-deepseek-api-key"
# DEEPSEEK_BASE_URL="https://api.deepseek.com/v1"


# --- Router-Einstellungen (optional, für später) ---

# Umgebung: development / staging / production
# Wird z.B. in config.py genutzt, um Debug-Optionen zu steuern.
ROUTER_ENV="development"

# Logging Level: debug / info / warning / error
ROUTER_LOG_LEVEL="info"

# Standard-Timeout (Sekunden) für Provider-HTTP-Calls
ROUTER_DEFAULT_TIMEOUT="60"

# CORS-Origins für lokale Entwicklung (z.B. wenn du später ein Web-Frontend nutzt)
# Mehrere Origins per Komma trennen.
# ROUTER_ALLOW_ORIGINS="http://localhost:3000,http://localhost:5173"
